# 應用程式執行說明（3 天衝刺版）

這是你專題的主應用程式：一個整合股價、技術指標、PTT 輿情的互動式分析儀表板。

## 怎麼執行（在你自己的電腦上）

1. 把 `app.py` 放進你那個 `跑程式\files` 資料夾，**和那些 CSV 放在一起**
   （stock_0050_TW.csv、stock_2330_TW.csv、stock_TSM.csv、
    daily_sentiment.csv、ptt_sentiment.csv）。

2. 安裝套件（只需一次）：

   ```
   python -m pip install streamlit plotly pandas
   ```

3. 在那個資料夾啟動 app：

   ```
   streamlit run app.py
   ```

4. 瀏覽器會自動打開（網址 http://localhost:8501）。
   如果沒自動開，手動把那個網址貼到瀏覽器即可。
   要關閉時，回終端機按 Ctrl + C。

## 這個 app 有什麼

- **關鍵指標卡**：最新收盤、漲跌、RSI 狀態、PTT 輿情情緒
- **互動股價圖**：K 線 + MA5/MA20 均線 + 成交量，可縮放、可開關
- **RSI 指標圖**：標出超買(70)、超賣(30)區
- **輿情視覺化**：每日情緒長條圖 + 逐篇情緒分布
- **趨勢傾向判斷**：結合 RSI、均線、輿情的規則版綜合傾向
  （已誠實標示為「示意版」，正式 ML 模型待資料累積後升級）
- 左側可切換標的（0050 / 台積電 / 台積電 ADR）、調整顯示天數

## Demo 給老師時的講法（建議）

1. 先講系統架構：資料採集 → 情緒分析 → 視覺化整合，四種資料源已打通。
2. 現場切換標的、縮放圖表，展示互動性。
3. 指著輿情情緒圖說明：已用本機中文 BERT 模型對 PTT 文章做情緒分析。
4. 講趨勢傾向時，主動點出「這是規則版示意，ML 模型是下一階段」——
   誠實說明限制與後續規劃，比假裝有完整模型更專業、更得分。
5. 對應你們規劃表：本 app 完成了 Phase 3 核心開發的 MVP，
   需求項目 1（股價視覺化）、2（輿情情緒）已實作，3（ML 模型）為後續。

## 如果時間還有餘

- 回 `fetch_ptt.py` 把 `PAGES_TO_CRAWL` 調大（如 100），分批多跑幾次，
  累積數月輿情資料後，就能訓練真正的機器學習漲跌模型，補進 app。
- 所有腳本與這個 app 都已寫好、可重複執行，資料一多就能無縫升級。

本系統為學術專題用途，所有內容僅供研究參考，不構成投資建議。
